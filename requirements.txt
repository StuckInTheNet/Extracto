# Core dependencies
PyMuPDF>=1.24,<2.0
opencv-contrib-python>=4.9,<5.0
numpy>=1.26,<3.0
joblib>=1.3,<2.0
scikit-learn>=1.4,<2.0
reportlab>=4.1,<5.0

# YOLO detection (for scanned documents)
ultralytics>=8.1,<9.0
torch>=2.0

# TableTransformer (for scanned financial tables)
transformers>=4.30
timm>=0.9

# Fuzzy matching
# (uses stdlib difflib — no extra package needed)

# Development
pytest>=8.0
pytest-cov>=4.1
ruff>=0.3
